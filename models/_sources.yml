version: 2

sources:
  # ──────────────────────────────
  #  Live schema
  # ──────────────────────────────
  - name: hkh_dev           # first arg in source()
    schema: hkh_dev         # Postgres schema

    tables:
      - name: tbl_terms
      - name: tbl_hurdle_tiers

  # ──────────────────────────────
  #  Seed / input schema
  # ──────────────────────────────
  - name: inputs            # first arg in source()
    schema: inputs          # ← this is where *property_inputs* & *capex_factors* sit

    tables:
      - name: property_inputs
      - name: capex_factors